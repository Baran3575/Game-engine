name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Engine tests
        run: node web/test/engine.test.js
      - name: Blueprint graph tests
        run: node web/test/graph.test.js
      - name: App boot test
        run: node web/test/boot.test.js
      - name: MCP server tests
        working-directory: mcp
        run: |
          npm ci --no-audit --no-fund
          npm test
